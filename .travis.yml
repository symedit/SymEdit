language: php

matrix:
    include:
        - php: 5.4
        - php: 5.5
        - php: 5.6
        - php: 7.0
    allow_failures:
        - php: 7.0

install:
    - composer --prefer-source install

before_script:
    - php app/console symedit:install --force --env=test

script:
    - phpunit
