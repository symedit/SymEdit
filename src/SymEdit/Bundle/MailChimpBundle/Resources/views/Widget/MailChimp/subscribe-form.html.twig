{{ form_start(form) }}
    <div class="input-group">
        {{ form_widget(form.email, { 'attr': {'placeholder': placeholder }}) }}
        <span class="input-group-btn">
            <button type="submit" class="btn btn-primary">{{ button_text }}</button>
        </span>
    </div>
{{ form_end(form) }}
