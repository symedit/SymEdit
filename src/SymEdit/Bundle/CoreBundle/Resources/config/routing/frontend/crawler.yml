robots:
    pattern: /robots.{_format}
    requirements:
        _format: txt
    defaults:
        _controller: SymEditBundle:Crawler:robots
        _format: txt

sitemap:
    pattern: /sitemap.{_format}
    requirements:
        _format: xml
    defaults:
        _controller: SymEditBundle:Crawler:sitemap
        _format: xml
