<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <!-- Page -->
        <service id="symedit.controller.page" class="%symedit.controller.page.class%">
            <argument>symedit</argument>
            <argument>page</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <!-- Post -->
        <service id="symedit.controller.post" class="%symedit.controller.post.class%">
            <argument>symedit</argument>
            <argument>post</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <!-- Category -->
        <service id="symedit.controller.category" class="%symedit.controller.category.class%">
            <argument>symedit</argument>
            <argument>category</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <!-- Image -->
        <service id="symedit.controller.image" class="%symedit.controller.image.class%">
            <argument>symedit</argument>
            <argument>image</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <!-- Settings -->
        <service id="symedit.controller.settings" class="%symedit.controller.settings.class%">
            <argument>symedit</argument>
            <argument>settings</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <!-- Widgets -->
        <service id="symedit.controller.widget" class="%symedit.controller.widget.class%">
            <argument>symedit</argument>
            <argument>widget</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>

        <!-- Slider -->
        <service id="symedit.controller.slider" class="%symedit.controller.slider.class%">
            <argument>symedit</argument>
            <argument>slider</argument>
            <argument />
            <call method="setContainer">
                <argument type="service" id="service_container" />
            </call>
        </service>
    </services>
</container>