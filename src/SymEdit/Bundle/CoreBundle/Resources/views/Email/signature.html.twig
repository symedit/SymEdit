  <address>
      <strong>{{ Settings.company.name }}</strong>
      {% set info = ['phone', 'email', 'address'] %}
      {% for piece in info if Settings.company[piece] is not empty %}
      <br> {{ Settings.company[piece] }}
      {% endfor %}
  </address>