{{ mopa_bootstrap_menu('tree', {
    root_options: {
        childrenAttributes: {
            class: ulClass|default('main nav navbar-nav')
        }
    },
    template: '@SymEdit/Menu/navigation.html.twig'
}) }}
