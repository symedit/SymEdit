{% macro delete_button(id, label, class) %}
{% set class = class|default('btn btn-warning') %}
<button data-target="#{{ id }}" data-message="{{ 'admin.resource.confirm_delete'|trans }}" data-toggle="form-forward" class="{{ class }}" type="button">
    {{ icon('trash') }}
    {{ label|trans }}
</button>
{% endmacro %}

{% macro delete_form(id, path) %}
<form id="{{ id }}" method="post" class="form-hidden" action="{{ path }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="confirmed" value="1">
</form>
{% endmacro %}
