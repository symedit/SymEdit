{% extends '@SymEdit/Admin/base.html.twig' %}

{% block content_container %}
<div class="container-fluid">
    <div id="content" class="has-sidebar">
        {{ block('content') }}
    </div>
</div>
{% endblock %}


{% block content %}
{% if form is defined %}
{{ form_start(form, {attr: {class: 'form-horizontal'}}) }}
{% endif %}

    <div class="row">
        {% block sidebar %}
        <div id="sidebar" class="col-sm-3 col-md-2">
            {% block tabs %}
            {% if form is defined %}
            {{ form_tabs(form) }}
            {% endif %}
            {% endblock %}

            <div class="actions">
                {% block form_actions %}{% endblock %}
            </div>
        </div>
        {% endblock %}


        <div id="tab-content" class="col-md-10 col-sm-9 col-md-offset-2 col-sm-offset-3">
            {% block tab_content %}
            {% if form is defined %}
            {{ form_widget(form) }}
            {% endif %}
            {% endblock %}
        </div>
    </div>

{% if form is defined %}
{{ form_end(form) }}
{% endif %}
{% block extra %}{% endblock %}
{% endblock %}