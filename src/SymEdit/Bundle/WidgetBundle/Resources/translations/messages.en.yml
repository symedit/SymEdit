symedit:
    form:
        widget:
            tab:
                basic: Basic
                options: Options
            actions:
                new: Create New Widget
                create: Create Widget
                save: Save Widget
                delete: Delete Widget
            basic:
                title: Title
                name:
                    label: Name
                    help: Numbers, letters, underscores, or hyphens.
                area: Area
                visibility:
                    label: Visibility
                    include_all: Include on ALL Pages
                    include_only: Include ONLY on specified
                    exclude_only: Exclude ONLY on specified
                associations: Associations
            options:
                template:
                    help: Override the default template to customize rendering
